//! Handles both encrypt and decrypt submodules

use crate::{CipherConfig, DecryptOpt, EncryptOpt};

/// Handles the encrypt submodule
pub fn encrypt(cfg: &CipherConfig, encrypt_opt: EncryptOpt) -> anyhow::Result<()> {
    Ok(())
}

/// Handles the decrypt submodule
pub fn decrypt(cfg: &CipherConfig, decrypt_opt: DecryptOpt) -> anyhow::Result<()> {
    Ok(())
}
